name: Site CI

# Verifies that the repo is actually wired together, which nothing else checks:
# every link in every HTML and Markdown file resolves, the project manifest that
# the hub and the in-app nav both read is intact, and — in a real Chromium — the
# hub lists every project, each app boots clean, and the nav in each app leads
# back to the hub. A broken cross-link is a bug here, not a documentation nit.

on:
  push:
  pull_request:
  workflow_call:

concurrency:
  group: site-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  links:
    name: Link & manifest check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      # No dependencies — pure Node, so this job is seconds long.
      - name: Check every local link resolves
        run: node scripts/check-links.mjs
      - name: Check every cross-project dependency is declared
        run: node scripts/check-exchange.mjs
      - name: check-exchange's own tests
        run: node scripts/tests/check-exchange.test.mjs
      # SCRIPT FORGE's reader/writer/exporters are plain modules, so they can be
      # checked here without a browser — seconds, no dependencies.
      - name: SCRIPT FORGE logic tests
        run: node film/tests/film-logic.test.js
      # NAME FORGE's word bank and engine are plain modules too: these prove
      # the 1000 + 1000 word counts, no duplicates, and that the word order is
      # really rolled rather than always adjective-first.
      - name: NAME FORGE logic tests
        run: node names/tests/names-logic.test.js
      # CODA PICS paints onto a recording canvas, so its whole pipeline —
      # reading the words, painting the scene, finishing it in every style —
      # is checked here without a browser too.
      - name: CODA PICS logic tests
        run: node coda-pics/tests/coda-logic.test.js
      # The Google Photos connector is the one thing in the arcade that talks to
      # a network, so its whole flow — PKCE, the state check, the token swap,
      # polling, paging, fetching pixels — is driven against a fake Google.
      - name: CODA PICS Google Photos connector
        run: node coda-pics/tests/gphotos.test.js
      # NEON CELLS' level generator, combat maths and save file are plain
      # modules too — level reachability is checked over hundreds of seeds here,
      # which is the check that stops a run ending behind a wall.
      - name: NEON CELLS logic tests
        run: node cells/tests/cells-logic.test.js

  browser:
    name: Site smoke test (Chromium)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install Playwright
        working-directory: music/tests
        run: npm install --no-audit --no-fund
      - name: Install Chromium
        working-directory: music/tests
        run: npx playwright install --with-deps chromium
      - name: Drive the hub and every app
        run: node scripts/smoke-site.cjs
      - name: SCRIPT FORGE end to end (write, export, save)
        run: node film/tests/film-browser.test.js
      - name: NAME FORGE end to end (roll, flip, batch, export, save)
        run: node names/tests/names-browser.test.js
      - name: CODA PICS end to end (paint, restyle, keep, save)
        run: node coda-pics/tests/coda-browser.test.js
      # Proves the pictures have not silently changed. A prompt and a seed are
      # promised to give one picture forever — the gallery and every shared
      # link rest on it — so a change here is a failure until it is re-recorded
      # on purpose.
      - name: CODA PICS pictures are unchanged
        run: node coda-pics/tests/visual.test.js
      # ...and the same flow again through the real page: real sessionStorage, a
      # real redirect out and back, real image decoding, and a picture painted
      # from photographs that came in through the connector.
      - name: CODA PICS Google Photos through the page
        run: node coda-pics/tests/gphotos-browser.test.js
      # A game can pass every unit test and still throw on frame two, so this
      # plays one: a real run, every biome, every screen, and a death.
      - name: NEON CELLS end to end (a real run)
        run: node cells/tests/cells-browser.test.js

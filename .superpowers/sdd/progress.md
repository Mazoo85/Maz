# Subagent-driven development — progress ledger

Plan: docs/superpowers/plans/2026-09-11-the-exchange.md
Spec: docs/superpowers/specs/2026-09-11-the-exchange-design.md
Branch: claude/feedback-loop-brainstorm-22rx2c
Base: 29301657 (the plan commit)

Tasks listed complete here are DONE — do not re-dispatch them.

(Previous project's ledger preserved in progress-score-to-picture.md)

Task 1: complete (commits 8ad50d22..256b5a71, review clean after one fix wave — Critical: `consumes[].contract` reached path.join unguarded, so a non-string crashed the checker with an uncaught TypeError instead of its own named error, the exact "untrusted input" guarantee the task exists to provide; Important: the shared/projects.js dynamic import had no try/catch and assumed PROJECTS was an array. Both were the container-validated-but-not-its-elements class, now shipped 7 times in this repo. The implementer also caught a vacuous test in the plan's own supplied code — it passed because the expected string appeared inside a TypeError stack trace. Minor carried to Task 2: check-exchange.mjs imports readdirSync/statSync/relative and defines SKIP_DIRS/INFRASTRUCTURE, all unused until Task 2 claims them.)
Task 2: complete (commits 9478eeac..ca1f48e4, review clean after one fix wave of SIX findings — 2 Critical: `publishes[id].files` truthy-but-not-an-array crashed at want.filter, and `consumes[].page` non-string crashed inside path.join, both the container-not-elements class again; 2 Important vacuous tests: the "missing declared file" test asserted a substring that the unrelated wrong-order message also contains, and the reviewer found that with the FIRST declared file missing rather than the third, removing that guard made the checker silently exit 0 on genuinely missing coupling; the "own files" test never reached the owner guard because its reference had no `../`. 1 Important false positive: `../../x` parsed the project as `..` so any reference up two levels always failed as undeclared. 1 Minor: query strings/fragments not stripped. The first implementer reported 2 mutations surviving and explained them away as "alternate detection paths"; the review confirmed both were genuinely vacuous. Re-review independently reproduced all 9 mutations rather than trusting the report.)
  CARRIED MINOR for final review: scripts/check-exchange.mjs CROSS_REF is anchored on `../`, so a page at repo depth 0 (the hub index.html) referencing a sibling project without a `../` prefix is invisible to rule 5b. Inherited from the plan's own regex, does not bite today. A naive fix would flag the hub's legitimate <a href="music/"> navigation links — distinguishing coupling from navigation means scanning only <script src> and <link href>, not <a href>.
Task 3: complete (commit 2c5855f7, review clean FIRST PASS — no fix wave. exchange.py transcribed unchanged; the reviewer confirmed byte-identical to the brief and independently built an adversarial battery (non-dict publishes entries, unhashable project/id values reaching set/in, top-level list/str/int) finding no container-not-elements gap. The implementer, asked to audit the brief's own code before transcribing, found and fixed TWO vacuous tests in the brief's test suite: the "consumer is not its own consumer" test never exercised its guard because the fixture's consuming project published nothing, and the sort test was hash-order-dependent so tuple(sorted(out))->tuple(out) survived intermittently — widened to 5 consumers, verified 0/20 survivals across distinct PYTHONHASHSEED values. 317/317 forge tests under -W error.)
  NOTE: instructing the implementer to read the plan's own code adversarially before transcribing is what made this the first clean pass. Tasks 1 and 2 both needed fix waves for defects that originated in the plan, not the implementation. Keep doing this for the remaining tasks.
Task 4: complete (commit f37ceb0a, review clean FIRST PASS — no fix wave. The feature now does its job: all_commands('music/', root) returns music's test then film's test, verified against the real repo. load() is called unconditionally BEFORE any branch on project, so no zone — including project-is-None zones and zones absent from ZONE_PROJECT — can get a command list from a missing or malformed declaration. run_checks returns ok=False, which orchestrate reads as verify_failed/checks:red, so a broken declaration can never surface as checks:green. Own-before-downstream ordering is structural, not dict-iteration-dependent. The implementer found TWO more vacuous tests in the brief: the "missing declaration raises for a zone with no consumers" test used crew/tests/ whose project is non-None so it never exercised its mutation, and the dedup test was masked by consumers_of's own upstream set-dedup. It also had to extend test_orchestrate.py and test_orchestrate_real_git.py, not in the brief's file list, because run_checks now fails closed for every zone — the reviewer confirmed these were fixed by giving each test a real declaration, not by weakening run_checks or loosening an assertion. 330/330 under -W error.)
Task 5: complete (commit 09398d2d, review clean FIRST PASS — no fix wave. The exchange_ok gate sits after `considered += 1` and before the zone check, pinned by a test that would fail if moved. is_loadable is called once per run at each of the two call sites, not per candidate. The reviewer confirmed verify.py and checks.py are byte-for-byte untouched — the safety guarantee was NOT moved into DECIDE's permissive default, which was the Critical risk for this task. The implementer strengthened the brief's `test_a_good_exchange_changes_nothing`, which compared two whole result dicts containing a wall-clock `generated_at` field — flaky, and weakly constraining; it now pops that field and asserts a concrete chosen source. It also added the Task-4 `_exchange` helper to 7 more pre-existing test_orchestrate.py tests; the reviewer verified each hunk adds only that one line with no assertion touched. 333/333 under -W error. End to end on the real repo: good declaration picks the robots.py job in zone scraper/; declaration moved aside gives config_error == considered == 98 and picks nothing.)
Task 6: complete (commits 1b49e1b6..a0c4faa1, review clean after one fix wave — TWO Important documentation defects, both originating in the plan's own prescribed text. (1) The guide said a missing/malformed declaration makes the Forge "record config_error and pick nothing", describing only the DECIDE-time early exit as though it were the whole story; if the file breaks AFTER decide and before verify — minutes, while Crew works — the night ends verify_failed / checks:red with a quarantine strike, after real cost. An operator reading that ledger entry would not have connected it. (2) "A zone's checks now include everything downstream of it" was stated without its precondition: all_commands does PROJECT_CHECKS.get(consumer, ()), so a declared consumer with no PROJECT_CHECKS entry contributes ZERO commands and checks:green would overstate again — the same trivial-pass hazard the guide's own preceding paragraph already names for zones. Both fixed; re-review verified every outcome string and the quarantine behaviour against orchestrate.py rather than the report.)
  CARRIED MINOR for final review: decide.py's `config_error` skip counter is overloaded — incremented both for a broken exchange declaration and for a candidate whose kind is missing a weight in config.weights (KeyError in score_one). An operator seeing nonzero skipped.config_error cannot tell which. Pre-existing labelling ambiguity, not introduced by this branch.
  CARRIED MINOR for final review: `.superpowers/` is absent from scripts/check-links.mjs SKIP_DIRS, so the gitignored scratch reports under .superpowers/sdd/ are scanned by the link checker locally. Harmless on CI (the files do not exist there), but it produced a local-only false failure during Task 6 that the implementer worked around by editing a scratch file.

ALL SIX TASKS COMPLETE. Next: final whole-branch review.

FINAL REVIEW ROUNDS (five whole-branch reviews, each found one more of the same defect class — a change the Forge accepts and records checks:green for while running nothing that covers it):
  R1 (5 Important, commits 2b6b9ff8 + 316f02ae): checks keyed to the CHOSEN zone not the changed files, so a docs/-scoped task that also edited music/js/composer.js ran ZERO commands and recorded green; a producer with no own tests contributed nothing downstream (madlibs/, shooter/ mapped to None — directly on the spec's own stated growth path); a self-contradictory ledger note ("nothing scored above the floor" beside below_floor:0); the coupling rule flagged <a href> navigation while missing real <script> coupling from the depth-0 hub; a declaration could name files outside its own project.
  R2 (2 Important, commit a691297a): a safe zone absent from the hardcoded ZONE_PROJECT map verified nothing silently — reachable by following the guide's own rollout advice, and by a one-character forge.json typo ("music" for "music/"); a docs/-only change skipped the whole-repo exchange gate entirely (reproduced: Forge creates docs/demo.html with undeclared music scripts, VERIFY green, CI red).
  R3 (1 Important, commit 40fe421e): of the five declared files, synth.js and engine.js were exercised by NOTHING — replacing both with unparseable text left everything green. Fixed by parse-checking every published file and making film's contract test load all five (the audio files load fine in Node; they touch AudioContext only lazily).
  R4 (2 Important, commit cd3ce85c): the escalation — a PARSEABLE change still passed: renaming Player->Sequencer made the film play silently with no score, green. Also gutting a file passed while its global survived. Plus a nightly-failing regression R3 introduced (.venv dropped from SKIP_DIRS) and a dist/ blind spot (dist is not gitignored).
  R5 (1 Critical, commit 9da1a8f7): the hand-written member allowlist stopped at load(), missing play/seek/stop/pause which film calls today — renaming play made the film not play AT ALL (TypeError, no try/catch), green. The reviewer's key point: the allowlist had failed twice for the same reason, so the fix was structural — film's contract test now DERIVES its asserted surface by scanning film/js/*.js for real call sites, resolving local aliases and the dynamic `this.player = new Play.Player(...)` binding, and fails loudly if the scan finds fewer than 8 sites. Also dropped 7 over-coupled assertions on music internals film never touches.

KNOWN REMAINING GAP (documented in docs/FORGE.md, not hidden): Synth.playNote gutted to a no-op is still green — right name, right type, right arity, no output. Closing it needs a real AudioContext or a far more granular fake than exists. The browser job on the PR remains the backstop.

Final state: 370 forge tests, 31 checker tests, 73 film tests, music's 1.4M assertions, check-links, check-exchange — all green.

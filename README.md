# Maz

This repo holds two things:

1. **Maz Engine** — a native **C++20 + Vulkan + SDL3** game engine, 2D-first but architected so
   3D drops in later. See **[`docs/ROADMAP.md`](docs/ROADMAP.md)** for the full build plan (the
   "massive list") and **[`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)** for the design.
2. **ZOMBOID: ANCHORAGE** — the browser game below, which is both a design reference and the
   engine's eventual flagship port target (roadmap Phase 13).

> **Codebase memory:** this repo is set up with a
> [codebase-memory MCP server](docs/CODEBASE_MEMORY.md) that gives Claude persistent,
> cross-session memory about the project. It starts automatically in Claude Code — no setup needed.

## Building Maz Engine

Requires a C++20 compiler, CMake ≥ 3.24, the **Vulkan SDK** (loader + `glslangValidator`), and
on Linux the X11 dev packages. SDL3 and GLM are fetched automatically.

```
cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug
cmake --build build
./build/bin/sandbox                 # window + animated clear color (needs a GPU + display)
./build/bin/sandbox --headless      # CI: init, run, exit cleanly with no display/GPU
ctest --test-dir build              # headless smoke test
```

The current milestone is **M0** — the walking skeleton: a window, a fixed-timestep loop, a
Vulkan clear-screen renderer, and clean shutdown. It degrades gracefully with no GPU/display so
it runs in CI.

## CJC Music Station

**CJC Music Station** is a native music-production app (a DAW) built on the engine's audio/UI layers,
aimed at FL Studio–level capability. It's a full groovebox/DAW: program patterns, arrange them into a
song, play them with real synths and samples, mix and process with effects, automate parameters, save
the project, and bounce to WAV.

`maz::audio` provides:

- **Instruments** — a synthesized drum kit (`DrumVoice` — kick/snare/hats/clap/**tom**/**cowbell**/**rimshot**/**crash cymbal**/**ride cymbal**/**shaker**/**clave**/**tambourine**/**conga**/**woodblock**/**bongo**/**triangle**/**808** (a long, pitch-gliding pure sub kick)/**zap** (a synthwave laser "pew" — a fast, wide downward pitch sweep)/**riser** (a reverse-cymbal noise sweep that swells up into a build-up/transition, its length set by the channel decay knob)/**808 snare** (a TR-808 snare — two tuned sine partials as the shell plus a longer snappy noise tail, the **snap** knob balancing shell↔wires)/**808 hat** (a TR-808 hi-hat — a dense cluster of six inharmonic square oscillators, bright and metallic, tightly gated)/**808 clap** (a TR-808 clap — three noise bursts ~9 ms apart plus a smeared noise tail, the signature stuttered clap)/**snap** (a dry finger snap — a single short, band-focused noise pop with a woody ~2.2 kHz resonance and a very fast decay, tighter and drier than the stuttered clap)/**timbale** (a high, ringing metal-shell Latin drum — a tuned ~300 Hz fundamental plus inharmonic shell-ring partials and a sharp stick attack, brighter than the pure-membrane tom and more tonal than the cowbell)/**agogo** (a bright, high-pitched metallic bell — a clear ~780 Hz fundamental plus inharmonic bell partials and a sharp strike, higher and more bell-like than the buzzy two-square cowbell)/**splash** (a short, explosive splash cymbal — as bright and noisy as the crash but with a fast, sharp decay, so it reads as a quick accent rather than the crash's long wash or the ride's pingy sustain)/**china** (a trashy, aggressive china cymbal — extra noise plus dense high inharmonic partials for a harsh metallic clang, its decay sitting between the short splash and the long crash)/**guiro** (a scraped Latin idiophone — bright noise "rasped" by a fast ~55 Hz amplitude ratchet that simulates the scrape crossing the ridges, giving the characteristic buzzing "brrrp", distinct from every other voice by its strong periodic amplitude gating rather than a steady noise wash), **per-channel selectable** per row, per-hit velocity, **choke groups**, **per-channel decorrelated noise** (each channel's noise generator is seeded distinctly, so two channels of the same drum type sum incoherently like real hits instead of doubling a bit-identical signal), per-channel **tuning** + **decay** + **drive/saturation** + **flam** + **pitch-envelope depth / "punch"** + **pitch-envelope time** (together shape the kick/tom pitch sweep — flat sub → snappy click → long 808 boom) + **tone** (a per-drum low-pass to darken bright hits, FL channel-filter style) + a per-drum **high-pass / low-cut** (tighten a boomy kick, shave rumble off hats/claps — the other half of a channel filter) + a snare **snap** (noise-vs-tone balance, from tom-like body to a crisp wire crack)); **two** polyphonic
  synths (lead + bass, `SynthInstrument`) with **subtractive** (a **three-oscillator** stack — seven selectable shapes per oscillator (sine / square / saw / triangle / **trapezoid** — a clipped triangle, warmer than a square yet brighter than a triangle / **step-sine** — a sine quantized to a discrete amplitude ladder for a lo-fi / chiptune tone / **rect-sine** — a half-wave rectified sine that keeps the fundamental but adds strong even harmonics for a bright, reedy/hollow tone) + detuned 2nd osc + **coarse-tunable** 2nd & 3rd oscs (+ **fine-detune** on osc3 for a fatter beating stack) + **ring mod** + sub +
  noise + a selectable-waveform, **-1/-2 octave sub** + **unison** supersaw stack + **2nd & 3rd oscillators each with their own selectable waveform** (3xOSC-style, or linked to the primary), a resonant state-variable **filter** — **low-pass/high-pass/band-pass/notch** selectable, with a **12 / 24 dB/oct slope** switch (a steeper Moog-style rolloff) — with envelope + **velocity sensitivity** + **key tracking** + a **cutoff LFO** (wobble/auto-wah, with a selectable **shape** — sine/square/saw/triangle/trapezoid, so the wobble can step, gate, or ramp, plus a **sample & hold** random-stepped mode that jumps the cutoff to a new level each cycle for classic acid/IDM filter movement — **tempo-syncable** to note divisions for rhythmic filter gating) + a **tremolo/amp LFO** (with a selectable **shape** — sine/square/saw/triangle/trapezoid, so a square gives a hard on/off gate, plus a **sample & hold** random-stepped mode for a glitchy random tremolo; also **tempo-syncable** for a synth trance-gate) + a **tempo-syncable vibrato** (pitch LFO lockable to note divisions, with a selectable **shape** — sine/square/saw/triangle/trapezoid, so a square gives a two-pitch trill, plus a **sample & hold** random-stepped mode for random stepped pitch — chiptune/arp character) + a **dedicated filter envelope** (its own ADSR sweeping the cutoff, bipolar depth in Hz) + **filter drive** (pre-filter tanh overdrive for analog growl) + a **velocity→attack** (soft keys swell in slowly, hard keys snap — the classic velocity-to-envelope expression)), **FM** (with operator **feedback** + **velocity→index** for touch-sensitive brightness + a **selectable modulator waveform** — a saw/square/triangle operator injects far richer sidebands than the default sine for grittier DX-style tones), **wavetable** (a morphing
  single-cycle table with **selectable frames**, scanned by an envelope- **and LFO**- **and velocity**-sweepable position), **pluck** (a Karplus-Strong physical-modelled plucked string — a noise burst fed through a tuned, damped delay line for natural guitar/harp/koto-style plucks, with a **damping** control from bright/long-ringing to soft/muted, and a **pluck position** that combs the excitation — centre plucks go hollow (even harmonics nulled), bridge plucks stay bright), **organ** (an additive tonewheel-organ engine — **8 harmonic drawbars** summed for Hammond-style tones, with **key-click percussion** — a fast-decaying 2nd/3rd-harmonic attack ping), and **phase distortion** (a Casio CZ-style engine — an amount knob warps the sine's read-phase through a two-segment map, morphing a pure sine into a bright, saw-like digital tone distinct from FM/wavetable) engines, plus **portamento/glide** (with a **legato-only** mode — glide just on overlapping notes), **analog drift** (per-note detune for analog warmth), **start-phase randomization** (each note begins its oscillators at a random phase for analog-style variation), **vibrato** (with an **onset delay** that fades it in on sustained notes), **tremolo** (amplitude LFO), a **pitch envelope**, a **noise-attack transient** (a fast-decaying noise burst on each note's onset for a percussive click/chiff attack on plucks, pads, and basses), **oscillator hard sync**, **pulse width (PWM)** with a **PWM LFO** (lush sweeping duty cycle), a per-instrument **octave** shift, a **wavefolder** (a West-coast/Buchla-style waveshaper that drives the summed oscillators past ±1 and reflects them back on themselves, injecting sweeping upper harmonics as it's pushed — applied per-voice before the filter, so it tracks the note's own dynamics, a timbre distinct from the tanh filter drive and the phase-distortion engine), and a **mono** (single-voice) mode;
  and a **`Sampler`** (WAV playback — 16/24/32-bit PCM and 32-bit float files, any channel count — pitch-shifted per note, with **reverse**, **loop** (incl. **ping-pong** bounce and an adjustable **loop region** — sustain just the body — with a **loop crossfade** to smooth the seam), a **beat slicer** (map N equal slices across the keyboard, Slicex-style), **start-offset**, a full **amp ADSR envelope** (attack/decay/sustain/release volume shaping — plucks, swells, held pads), a **velocity→volume sensitivity** (from fully dynamic down to velocity-independent one-shots), a **velocity→attack** (soft hits swell in slowly, hard hits snap — dynamic sample swells), a **velocity→start** (soft hits begin further into the sample, skipping the sharp transient for mellower ghost-notes while hard hits keep the full attack), a resonant **multimode filter** (per-voice cutoff + resonance, **low-pass/high-pass/band-pass/notch** selectable — thin a loop with a high-pass, isolate a band, notch a resonance — with its own **filter envelope** sweeping the cutoff and a **cutoff LFO** (a shared wobble/auto-wah on sampled material, with a selectable **shape** — sine/triangle/square/saw, so a square gives a hard filter *gate* — **tempo-syncable** to note divisions for a beat-locked wobble or gate) and **velocity → cutoff** for touch-sensitive brightness and **filter keyboard tracking** so high notes stay bright), a per-voice **drive** (tanh saturation to warm a sample or add grit to drums), a **pitch envelope** (each note starts up to ±36 st away and slides to its true pitch — zaps, risers, 808 glides), a **monophonic mode** (last-note priority for tight mono sampled bass/leads), a **portamento / glide** (a new note slides its read speed from the previous note's pitch to its own, with an optional **legato-only** mode — the classic mono sampled bass/lead slide), a **key-tracking toggle** (off = every key retriggers the sample at its natural pitch, the classic one-shot / drum-sampler mode), **fine tune**, **normalize**, and **fade in/out**).
- **Sequencing** — a `Sequencer` channel rack (step grid with per-step velocity, **probability**, and **ratcheting**), **add-able instrument channels** (beyond the fixed lead + bass, create any number of extra melodic channels — each with its own synth engine and a per-pattern piano-roll lane, selectable in the Piano Roll's lane picker with a **+ Channel** button — the first step toward a fully flexible, unlimited-channel rack; each extra channel has its own **gain**, **pan**, and a **target bus** (route it through the drums/lead/bass insert strip and any submix group), so it sits in the mix independently — and each extra channel's notes carry the **full per-note attribute set** (probability, fine-tune, roll/ratchet, micro-timing nudge, slide, and cutoff/Mod X), routed through the same sample-accurate scheduler as the lead/bass lanes, and included in **MIDI export/import** — each extra channel round-trips on its own MIDI channel), **two** built-in `PianoRoll`
  lanes (lead + bass) with a **chord tool** (maj/min/7ths/sus/6ths/9ths/**dim7**/**m7♭5**/**11th**/**13th**/**power (5)**/**mMaj7**/**7♯5 (aug7)**/**6/9** — 23 qualities), **per-note probability**, **per-note fine tune** (micro-detune in cents), **per-note roll/ratchet** (a note re-strikes 1–8 times across its first step for melodic rolls/stutters — Shift+scroll a note), **per-note trig conditions** (Ctrl+Shift+scroll to set a note's loop stride 1–8 — the note fires only every Nth pattern loop, deterministic melodic fills/variation; the melodic counterpart of the drum-step trig conditions), **per-note micro-timing nudge** (Ctrl+Alt+scroll to delay a note's onset up to 95% of its step for a behind-the-beat/humanised feel — the melodic counterpart of the drum-step nudge, delivered sample-accurately), **per-note filter cutoff (FL "Mod X")** (Ctrl+Alt+Shift+scroll a note to shift the synth's filter cutoff for just that note, ±8 octaves — routed per-voice so each note carries its own brightness, the classic per-note filter accent for expressive basslines/leads; needs the synth filter engaged, and it survives a slide), **per-note slide** (TB-303-style portamento — Alt+scroll a note: the mono synth glides into it from the previous pitch *without* retriggering the amp *or filter* envelope, the classic acid-bass slide, and it slides even with the global glide at 0; both the lead and the bass synth expose a **Mono** toggle to enable it), **quantize** (adjustable strength) + **quantize note lengths** (snap durations to the grid), **scale-snap** (snap notes to major/minor/modes/pentatonic/blues/whole-tone/**phrygian-dominant**/**hungarian-minor**/**double-harmonic (Byzantine)**/**neapolitan-minor**/**hirajoshi**/**in-sen**/**egyptian**/chromatic — 21 scales, incl. exotic Japanese pentatonics), **strum** (roll stacked chords), **harmonize** (thicken a whole melody into chords — add each chord's tones over every note), **legato** (glue notes to the next), **invert** (mirror the melody around a pivot), **reverse** (flip the rhythm in time), **humanize** (randomize velocity + timing + note lengths), **humanize micro-timing** (random sub-step nudges that loosen the feel while keeping every note on its step — distinct from the whole-step timing randomize), **velocity ramp** (linear crescendo/decrescendo across the phrase), **scale dynamics** (compress or expand every note's velocity around the phrase's own average — tame an over-dynamic line or exaggerate it), **chop** (split notes into evenly-spaced repeats — rolls/stutters), **echo** (bake a rhythmic delay into notes — each note gets N decaying repeats at a set spacing), **flam** (add a quiet grace note just before each note — the classic flam/grace ornament), **arpeggiate** (bake a chord into a printed up/down/up-down arpeggio of notes), **transpose** (shift all notes by semitones), **transpose in key** (diatonic — shift by scale degrees, staying in the chosen key/scale), **limit** (fold out-of-range notes into a chosen pitch window by octaves, preserving pitch class — fit a line to an instrument's range), **mutate** (scale-aware randomize — move a random subset of notes by up to ±N scale degrees for generative melodic variation that stays in key, deterministic per seed), **shift** (nudge the whole phrase earlier/later in time, wrapping), **stretch** (time-scale the phrase faster/slower), **gate** (scale note lengths for staccato/legato without moving starts), and **duplicate** (repeat the phrase), **per-step pitch** (a channel-rack graph-editor pitch row — Ctrl+scroll a step to tune that hit ±24 semitones), **per-step micro-timing nudge** (Ctrl+Shift+scroll to push a single hit up to 95% of its slot later for a laid-back, behind-the-beat feel — a finer-grained cousin of swing), **per-step trig conditions** (Alt+scroll to set a step's loop stride 1–8 — fire only every Nth pattern loop, the Elektron/FL-style conditional trig for fills and long-form variation; deterministic, distinct from the random per-step probability), per-channel **step-row rotate** (shift a groove around the bar) and **Euclidean fill** (evenly distributed pulses), a **resizable pattern length** (8–64 steps) with adjustable **steps-per-beat** (16ths, triplets, 32nds), **multiple named + clonable patterns + a playlist** (looping or play-once, with an optional **loop region** to cycle a section) to build songs, **per-pattern swing** (each pattern grooves on its own), **groove templates** (one-click named per-step micro-timing feels — Straight / Swing 16th / Swing 8th / Laid-back / Hard swing — stamped onto every channel's step nudges **and the melodic lanes' per-note nudge** for an instant MPC-style groove across the whole pattern, richer than the single swing knob), **drum micro-timing humanize** (random per-step nudges for a loose "drummer feel" — the timing counterpart of the velocity humanize), **per-pattern transpose** (each pattern carries its own key offset on top of the global transpose, so a song can change key between patterns — verse in C, chorus up a step — melody/bass/arp only, drums unaffected), **per-pattern tempo** (each pattern carries its own tempo multiplier — 0.25×–4× the song BPM — so a chained song can drop a section to half-time or push it to double-time without touching the master tempo), **humanize**,
  an **arpeggiator** (up/down/up-down/**down-up**/random/as-played/**chord** (rhythmic full-chord stabs), 1–4 octave range, adjustable **gate length** for staccato/legato, and a **rate** of 1–8 steps per note to run slower than the grid), a **global transpose**, a **metronome** (with an adjustable **level**) with **count-in**, and **sidechain** ducking (with a selectable trigger channel, plus **attack** and release times for an instant hard pump or a softer rounded duck).
- **Mixing** — per-channel volume/mute/solo/**pan**, **pan for the lead & bass buses**, true **stereo**, **per-bus insert strips**
  (drums / lead / bass, each with its own **noise gate** + high-pass + **transient shaper** (per-bus attack/sustain punch) + EQ + distortion + compressor + a **Haas stereo widener** (widen the lead wide, keep the drums centred) + gain/pan/mute/**solo** + **per-bus reverb & delay aux sends** + **routable output** — each bus can go straight to the master or into a **submix group**), **submix/group tracks** (add-able group buses, each a full insert strip — EQ/drive/compressor/gate/transient/widener + gain/pan — that any instrument bus can be routed into and shaped together before the master, the FL "send to group" mixer routing, the first step toward a fully flexible mixer), a master effect
  chain (parametric EQ (low shelf + **three** independent sweepable mid bells + high shelf), a resonant **multimode DJ filter** (low-pass/high-pass/band-pass with a sweepable cutoff + resonance + a **drive** for the classic acid/driven-filter growl — filter-sweep build-ups), tilt EQ (with an adjustable **pivot** frequency), aural exciter (high-band harmonic enhancer, with an **even/odd harmonic** switch — transistor-style odd-harmonic edge or a tube-style 2nd-harmonic octave sparkle), low-pass, high-pass, distortion with selectable curves (soft/hard/fold/sine/**tube** — asymmetric, even-harmonic warmth) + a **bias/asymmetry** control (offsets the signal into the shaper so the wave clips unequally, adding *even* harmonics — tube-like warmth — to any curve, DC-blocked so the output stays centred) + a post **tone** low-pass + an **output-level** trim (drive hard, then gain-stage the result back down), a **guitar amp + cabinet sim** (Hardcore-style — a preamp overdrive into a speaker-cabinet voicing: low-cut to shed flub, a **presence** mid bite bump, and a **tone** high-cut for the speaker rolloff), tape saturation (with **wow & flutter** pitch wobble), ring modulator (with a **selectable carrier waveform** — sine/square/saw/triangle, the richer shapes adding many more sidebands for a brighter, harsher clang), **pitch shifter** (a time-domain ±24-semitone shift with a dry/wet mix for detune/harmony + a **feedback** that re-shifts each pass, stacking the interval into a cascading spiral — Shepard-like octave risers, metallic staircases at odd intervals), **frequency shifter** (single-sideband Bode-style — shifts every partial by a fixed ±Hz via a Hilbert transform, turning harmonic tones inharmonic/metallic; small shifts phase/shimmer, large shifts clang), bitcrusher (bit-depth + sample-rate crush with a post **tone** low-pass to smooth the aliasing fizz, Squeeze-style), noise gate (with hold + a **sidechain/key high-pass** so booming lows don't false-open it),
  compressor (soft-knee, parallel/dry-wet mix, **sidechain high-pass** so lows don't pump it, **auto makeup gain** derived from threshold/ratio, a **peak / RMS detection** switch — RMS follows the average level for smoother "glue" compression that ignores brief transients — a **stereo-link** toggle (linked keeps the image centred; unlinked reduces each channel independently), **lookahead** — delays the audio so the gain is already engaged before a transient hits, catching peaks a plain compressor overshoots, live **gain-reduction meter**), 3-band multiband compressor (Maximus-style — independent low/mid/high threshold + ratio over two crossovers, exact-reconstruction band split), transient shaper (attack/sustain designer), a **multiband transient shaper** (independent attack/sustain per low/mid/high band over two crossovers — snap the drum highs while tightening the low sustain, exact-reconstruction band split, transparent when flat), de-esser (frequency-selective high-band compression), a **dynamic EQ** band (a fully-parametric bell — frequency + Q — that cuts *or* boosts only when the band crosses a threshold: a tunable, general de-esser for taming a resonant note, or an upward lift that adds air/presence dynamically, dB-accurate and transparent until it engages), chorus (with **feedback** for a deeper, flanger-edged voice + a **stereo width** control from mono to extra-wide + a **voices** control — 1 for a single-voice Juno-style chorus up to 3 for a lush ensemble/dimension sound), **vibrato** (a fully-wet swept delay for pure periodic pitch wobble — no dry blend — with **tempo sync** and a selectable LFO **shape** — sine/triangle/square (a two-pitch trill)/saw (a repeating pitch ramp)), **rotary** (a Leslie rotating-speaker sim — amplitude + Doppler-pitch + stereo rotation from one LFO, with slow/fast "chorale/tremolo" speeds and a **tube-preamp drive** for the overdriven-Leslie grit, pairs with the organ engine), flanger (with an **invert** switch for the hollow through-zero flange), and phaser (all three **tempo-syncable**; the phaser has a selectable **stage count** for deeper sweeps + a **stereo** mode that offsets the L/R sweeps 90° apart for a wide swirl), delay (with ping-pong + damping + a feedback **low-cut** for dub/analog echoes + **modulation** for warm BBD/tape pitch wobble + **feedback drive** — a tanh in the loop so the repeats warm and self-limit as they build, the tape/analog-echo character + **ducking** so the echoes bloom in the gaps of a loud dry signal + **tempo sync** to note divisions), stereo/dual delay (independent L/R times, each **tempo-syncable** to its own note division, with feedback **damping** + **low-cut** tone + a **ping-pong** mode that cross-routes the feedback so echoes bounce L↔R), a **reverse delay** (records the input in fixed-length chunks and plays each one back *backwards* as the wet echo — the psychedelic/EDM reverse echo where a sound swells up into its own transient, with feedback for repeating reversed grains and edge-fades to declick the seams), a **multi-tap delay** (Fruity Delay Bank-style — one input burst produces a rhythmic cluster of up to six echoes at successive multiples of the base time, each quieter than the last and panned alternately L/R so the pattern bounces across the stereo field — dotted/triplet cascades and rhythmic throws, distinct from the self-repeating feedback delays, plus an optional **feedback** that re-injects the longest tap so the whole cluster repeats into a sustaining rhythmic cascade), reverb (with pre-delay + width + freeze + ducking + wet-tail low-cut/high-cut tone + a **gated** mode for the classic 80s cut-off tail + **shimmer** — a parallel octave-up pitch-shift loop that stacks ascending octaves into the tail for the lush Eno/Valhalla ethereal halo — and **tail modulation**, an LFO-swept fractional delay on the wet output that makes the tail shimmer and de-correlate, the movement that separates a lush plate/hall from a static, metallic one), a **convolution reverb** (Fruity Convolver-style — convolves the signal with a finite impulse response for a dense, smooth, colourless tail with no metallic comb ringing; the IR is generated internally as decaying, tone-shaped, stereo-decorrelated noise whose length follows the decay knob, so you dial in a small ambience through to a long hall/plate — direct-form time-domain FIR, exact by construction), mid/side stereo
  widener, mono-bass maker, sub-bass generator (synthesizes an octave-down tone tracking the input's low end — club sub for weak kicks/basslines), an **octaver** (an octave-*up* generator — full-wave rectification adds a searing octave-higher harmonic layer, the classic Octavia/octave-fuzz lead doubler, with a tone control; the counterpart to the sub-bass), auto-pan (with **tempo sync** to note divisions for rhythmic panning + a selectable LFO **shape** — sine/triangle/**square** (trance-gate-style hard L↔R ping-pong)/**saw** (a one-way rotary sweep)), envelope filter / auto-wah (up **or downward**-sweeping, with a **dry/wet mix** for a parallel wah that keeps the body), comb resonator (with **damping** to mellow the metallic ring), a **chord resonator** (Fruity Vibed-style — a bank of six tuned comb resonators rooted at a chosen note and voiced to a chord (major/minor/dom7/min7/sus4/octaves), so exciting it with noise or percussion rings out the whole chord — turns unpitched hits into pitched harmony, with ring-length + damping + dry/wet), tremolo / trance-gate (with a selectable **shape** — sine/square/triangle/saw for smooth, hard-gated, ramped, or fading rhythms — and **tempo sync** to note divisions), a **16-step gate sequencer** (a drawable per-step volume pattern — stutters, ramps, syncopated gates — **tempo-synced** so the pattern spans a chosen note division, with edge smoothing to declick), a **beat-repeat / stutter** (the ratcheting core of Gross Beat / Slicex live — each fixed-length cell captures its first 1/N and replays that sub-slice N times to fill the cell, so a single hit stutters into a rhythmic roll; adjustable slice length, repeat count, and dry/wet, **tempo-syncable** so the cell locks to a note division, a pure passthrough at 1 repeat), formant (vowel) filter (5 vowels + a continuous **vowel morph** for talkbox A→E→I→O→U sweeps + a **formant/gender shift** — scale the formants up/down for a smaller/larger vocal-tract character without changing pitch), a **vocoder** (a 16-band channel vocoder with a built-in carrier — the input is the modulator, its per-band spectral envelope imposed on a buzzy **saw** (pitched robot voice) or **noise** (whisper) carrier, with adjustable carrier pitch + envelope release for the classic vocoder/talkbox effect), mid/side stereo widener (with **bass mono** — keeps lows centered while widening the highs), a **multiband stereo imager** (independent low/mid/high stereo width over two crossovers — widen the highs airy-wide while keeping the bass mono and tight, with exact-reconstruction band split), a **multiband saturator** (independent low/mid/high tanh drive over two crossovers — warm/fatten the lows and add presence to the highs without fizzing the whole mix, exact-reconstruction band split, bit-exact bypass at 0 drive), Haas stereo enhancer (widens even a mono source), utility (gain/phase-invert/mono + an output-stage **M/S width** trim — mono↔extra-wide without inserting a full widener), brickwall look-ahead limiter/maximizer (input gain + ceiling + release, guaranteed-ceiling output, live **gain-reduction meter**), a **leveler** (a slow auto-gain/AGC that rides a bus or master toward a target level — quiet passages lifted, loud ones eased down — with an adjustable response time and a symmetric max boost/cut bound; distinct from the compressor's fast threshold-based action), soft/hard clipper (instantaneous zero-latency ceiling — drive + hardness morphs from tanh saturation to a flat-top clip, for loudness — plus a **dry/wet mix** for parallel clipping that adds grit while letting transients poke back through)),
  parallel **aux send/return buses** (reverb + delay), a master **balance/pan**, and a master **limiter** with an adjustable ceiling.
- **Modulation** — automation of master-EQ-cutoff/FM/reverb/master/delay-mix/distortion-drive/stereo-width/**synth-filter-cutoff**/**synth-filter-resonance**/**bass-filter-cutoff**/**bass-filter-resonance**/**delay-feedback**/**reverb-size**/**bitcrusher-mix**/**pitch-shift** (semitone bends automated in real time)/**vibrato-depth** (pitch-wobble swells)/**ring-mod-frequency** (metallic carrier sweeps)/**chorus-mix** (wet-swell build-ups)/**reverb-damping** (evolving-space tone)/**frequency-shift** (through-zero metallic sweeps)/**rotary-rate** (Leslie chorale↔tremolo ramps)/**delay-time** (tape-stop/pitch-warp echoes)/**tremolo-depth** (gate the mix in over a build)/**master-filter sweep + resonance** (DJ filter build-ups)/**reverb-shimmer** (swell the octave-up halo in over a build)/**phaser-rate** + **flanger-rate** (ramp the sweep speed slow→fast)/**amp-drive** (ride the guitar-amp overdrive for build-up grit)/**auto-pan-rate** (accelerate the panning)/**comb-frequency** (metallic resonator pitch sweeps)/**octaver-amount** (swell the octave-up layer in over a build)/**convolver-mix** (ride the convolution-reverb space in/out)/**distortion-bias** (sweep the even-harmonic asymmetry)/**beat-repeat-mix** (ride the stutter wet in/out over a build or drop)/**formant-vowel** (sweep the talkbox vowel A→E→I→O→U for automated vocal filter sweeps)/**compressor-threshold** (ride the master compressor threshold for pumping/breathing builds)/**wavetable-position** (scan the lead synth's wavetable — the flagship Serum/FL-style timbre **morph** sweep, dark→bright over a build)/**synth-PWM** (sweep the lead square-wave duty cycle — the classic manual pulse-width sweep, hollow→buzzy)/**sampler-start** (modulate where each note begins in the loaded sample — Slicex-style glitch/stutter/scan)/**lead-bus volume**/**lead-bus pan**/**bass-bus volume**/**bass-bus pan**/**drum-bus volume**/**drum-bus pan**/**reverb-send**/**delay-send**/**master-pan** (the classic lead sweep + acid Q builds + bus volume rides on every bus + drum drops/builds + auto-pan + parallel-send risers/throws + whole-mix auto-pan), synced to the transport, from either an
  **LFO** (six shapes, a **phase offset** so lanes can run out of phase — e.g. a pan LFO a quarter-cycle ahead of a filter LFO — optionally **tempo-synced** to note divisions from 4 bars down to 1/8, or a **sample & hold** random-stepped mode for generative movement — random filter jumps, panning, pitch) or a drawn **automation clip** (FL-style breakpoint envelope, interpolated + looped, with per-point **curve tension** — each segment can ease-in or ease-out instead of a straight line, the classic FL tension handle, always passing exactly through both breakpoints).
- **I/O** — an `AudioEngine` that drives an SDL3 device *or* renders offline, **project save/load**
  (`.cjc`, with an in-memory string-snapshot variant that backs an **undo/redo** history — the `AudioEngine` exposes `snapshotForUndo()`/`undoEdit()`/`redoEdit()` over a bounded `UndoHistory`, capturing the *whole* project state — sequencer, mixer and automation — per edit, and the same snapshots enable project templates/copy-paste), **instrument presets** (`.cjcpatch` — save one synth channel's *entire* patch to a standalone file and recall it onto any synth channel or into another project, the FL channel-preset workflow; written and parsed by the very same code path as the in-project synth block, so a preset round-trips exactly and stays back-compatible as new synth fields are appended — "Save Patch"/"Load Patch" buttons in both the lead Synth and Bass Synth windows, each channel keeping its own preset file), WAV **bounce** (with optional **TPDF dithering** on the 16-bit export — the mastering step that trades quantization distortion on quiet tails for a benign, inaudible noise floor — and optional **peak-normalization** to −0.3 dBFS, a static one-gain lift to full scale that leaves dynamics untouched, plus **loudness (RMS) normalization** to a target dBFS (`--loudness N`) — lifts quiet masters to a consistent perceived loudness, gain-capped so the peak never clips, and **DC-offset removal** (`--removedc`) — subtracts each channel's mean to re-centre the mix (reclaims headroom, kills start/stop clicks), applied before the level stages, and a master **fade-in/fade-out** (`--fadein N` / `--fadeout N`, ms) — the classic song fade-out and a click-free start, applied last to shape the final master, and a selectable **16-/24-bit PCM or 32-bit float** depth — higher-quality masters, or lossless no-clip float for stems), **stem export** (per-bus drums/lead/bass WAVs through their track inserts, pre-master, in one pass), **MIDI export + import** (`.mid` in and out — the export carries the lead melody, the bass line, the drum grid as GM percussion — every drum type mapped to its own GM note by the channel's assigned drum, not a fixed position — and a tempo meta event so the project BPM is preserved both ways; a multi-pattern song exports its whole playlist arrangement back to back, not just the current pattern), session **recording** (output + SDL
  mic/line input capture, savable at the same 16/24-bit or float depth as the bounce), and **plugin hosting**: a native `.so` ABI
  ([`PluginApi.h`](engine/include/maz/audio/PluginApi.h), see `plugins/example_tremolo`), the
  open [CLAP](https://cleveraudio.org) format (`ClapHost`, see `plugins/example_clap`), **and
  [VST3](https://steinbergmedia.github.io/vst3_doc/)** (`Vst3Host`, see `plugins/example_vst3`) —
  hosted against Steinberg's **MIT-licensed** `pluginterfaces` headers only (no GPL `public.sdk`).
- **GUI** — a Dear ImGui interface (channel rack with a **Panic** all-notes-off button, piano roll, synth, bass, mixer with a **master peak/RMS level meter**, automation,
  arrangement, transport) rendered via Vulkan; a CI test drives it under software Vulkan.

See the **CJC Music Station track** in [`docs/ROADMAP.md`](docs/ROADMAP.md). All three plugin formats
(native ABI, CLAP, VST3) and live audio-input recording are implemented and, except for touching a
real audio device, exercised by the headless CI tests.

```
./build/bin/daw --headless --song --fm --auto --seconds 8 --wav song.wav   # full arrangement (bounce)
./build/bin/daw --headless --beat --melody --sample pluck.wav --wav sampled.wav  # melody via sampler
./build/bin/daw --headless --save song.cjc         # save the demo project to a .cjc file
./build/bin/daw --headless --load song.cjc --wav out.wav   # load a project and render it
```

```
cmake --build build                                    # builds the `daw` app too
./build/bin/daw                                        # window: channel rack + piano roll + test tone
./build/bin/daw --headless --beat --melody --seconds 4 --wav song.wav   # drums + melody to a WAV
./build/bin/daw --headless --beat --seconds 2 --wav beat.wav            # just the demo drum groove
./build/bin/daw --headless --seconds 1 --freq 440 --wav tone.wav        # a single oscillator tone
```

Flags: `--beat` (demo drum pattern), `--melody` (demo piano-roll melody), `--fm` / `--wt` (FM or
wavetable lead engine), `--bpm N` (tempo), `--freq HZ` (oscillator pitch), `--seconds N` (offline
length), `--wav PATH` (write the render), `--samplerate N` (render/export sample rate, e.g. 44100/48000/96000), `--bits N` (16/24-bit PCM or 32-bit float export), `--dither`, `--normalize`, `--mono` (downmix the bounce to one channel).
Headless mode needs no audio device, so CI verifies the synth, drums, and melodic sequencing.

---

# ZOMBOID: ANCHORAGE

A browser-playable, **Project Zomboid–style** open-world zombie survival game,
rendered as a **1990s SEGA 16-bit arcade title** and drenched in **neon**. The
map is a tile-built replica of **downtown & midtown Anchorage, Alaska** — real
avenues, real streets, and real building names.

No build step, no dependencies. It's pure HTML5 Canvas + vanilla JavaScript.

```
open index.html        # double-click it, or:
python3 -m http.server  # then visit http://localhost:8000
```

---

## The vibe

- **SEGA-90s presentation** — a "NEON GENESIS ARCADE presents" boot screen with a
  faux "SEEE-GAAA" jingle, a synthwave **PRESS START** title with neon sun and
  perspective grid, chunky 16-bit drop-shadow logotype.
- **Neon everything** — hot-pink / cyan / purple / acid-green HUD bars, glowing
  doorways, neon zombie eyes, muzzle flashes, scanlines + CRT vignette overlay.
- **Chiptune audio** — WebAudio square/triangle/saw blips and a looping
  Genesis-style bassline. Toggle with **L**.

## Survival (the Zomboid part)

Five decaying needs drive everything:

| Stat | Drains from | Fix it with |
|------|-------------|-------------|
| **Health**   | bites, starvation, dehydration, infection | bandages, painkillers, first-aid kits |
| **Fed**      | time | canned salmon, moose jerky, Moose's Tooth pizza, chips |
| **Hydro**    | time (fastest) | bottled water, soda, Kaladi coffee |
| **Energy**   | running, time | coffee, energy drinks |
| **Mood**     | exhaustion, low health | eating well, surviving |

- **Bites can infect you.** Infection climbs, drains health, and is only cured by
  **antibiotics** (loot the hospitals).
- **Day/night cycle.** Nights go dark (use **F** for the flashlight) and spawn
  faster, denser hordes — including **neon-pink sprinters**.
- **Loot** the glowing orange crates inside buildings with **E**. Loot tables are
  themed: police stations have guns & ammo, hospitals have meds, hardware stores
  have axes & crowbars, groceries have food.
- **8-slot hotbar inventory**, melee weapons with durability, and two firearms
  (M9 pistol, Mossberg 500) with ammo.

## Controls

| Key | Action |
|-----|--------|
| **WASD / Arrows** | Move |
| **Mouse** | Aim |
| **Left-click / Space** | Attack / fire |
| **E** | Loot crate / interact |
| **1–8** | Use / equip hotbar slot |
| **R** | Reload firearm |
| **Tab** | Inventory |
| **M** | Anchorage tactical map |
| **F** | Flashlight |
| **P** | Pause |
| **L** | Mute / unmute audio |

## The Anchorage map

A 160×140 tile grid. **Cook Inlet** to the west, the **Chugach** treeline to the
east, **Ship Creek** and the **Alaska Railroad Depot** along the north.

**Avenues (E–W):** Ship Creek · 1st · 3rd · 4th · 5th · 6th · 9th · Delaney Park
Strip · 15th · Northern Lights Blvd · Benson Blvd · Tudor Rd.

**Streets (N–S):** L · K · I · G · E · C · A · Cordova · Gambell · Ingra · Lake
Otis Pkwy · Boniface Pkwy.

**Named buildings include:** Hotel Captain Cook · 4th Avenue Theatre · Egan
Convention Center · Dena'ina Center · Alaska Center for the Performing Arts ·
Anchorage Museum · Z.J. Loussac Library · Snow City Cafe · Glacier Brewhouse ·
49th State Brewing · Nordstrom (5th Ave Mall) · Carrs · Fred Meyer · Title Wave
Books · REI · Sullivan Arena · Merrill Field · Providence & Alaska Native &
Alaska Regional Hospitals · Moose's Tooth · Bear Tooth · Chilkoot Charlie's ·
Spenard Builders Supply · Dimond Center · University of Alaska Anchorage ·
Anchorage Police Dept · and more. You spawn at **Town Square Park (5th & C)**.

## Project layout

```
index.html        # shell + script load order
css/style.css     # arcade-cabinet styling, scanlines
js/world.js       # Anchorage data: avenues, streets, named buildings
js/worldgen.js    # bakes data into a tile grid + loot containers
js/items.js       # item definitions + themed loot tables
js/audio.js       # WebAudio chiptune SFX + music
js/game.js        # engine: states, input, sim, combat, rendering, HUD
```

> A note on "exact replica": this is an affectionate, playable homage built from
> scratch — original code and art, with Anchorage's real geography and place
> names recreated as a game world. It is not Project Zomboid's code or assets.

## Tools

- [`scraper/`](scraper) — **maz-scrape**, a general-purpose, recipe-driven scraper
  for static HTML pages. Point it at a YAML recipe (field → CSS selector); it
  crawls (following pagination/links), extracts records, and writes JSONL/CSV/SQLite,
  politely by default. See [scraper/README.md](scraper/README.md).

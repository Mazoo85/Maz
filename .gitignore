.DS_Store
node_modules/
*.log

# Playwright is installed on demand for the browser suites
# (npm --prefix music/tests install), so its lockfile is local scratch.
package-lock.json

# C++ / CMake build artifacts
build/
build-*/
out/
cmake-build-*/
*.o
*.obj
*.spv
compile_commands.json
CMakeUserPresets.json
_deps/

# Python
__pycache__/
*.pyc

# Test scratch output
unit_scene_*.mazscene

# maz-scrape run state + default output directory
.scraper/
/scraped/
